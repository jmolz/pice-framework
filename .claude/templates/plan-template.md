# Feature: {Feature Name}

> Generated by `/plan-feature` — read CLAUDE.md and referenced files before implementing.

## Overview

{1-2 sentences: what this implements and why}

## User Story

As a {user type}, I want to {action}, so that {benefit}.

## Success Criteria

- [ ] {Verifiable criterion 1}
- [ ] {Verifiable criterion 2}
- [ ] All validation commands pass with zero errors
- [ ] No regressions in existing functionality

## Feature Metadata

- **Type**: New Capability / Enhancement / Refactor / Bug Fix
- **Complexity**: Low / Medium / High
- **Systems Affected**: {list}
- **New Dependencies**: {list or "none"}

---

## Context References

### Must-Read Files (READ BEFORE IMPLEMENTING)

- `path/to/file` (lines N-M) — Why: {pattern to mirror, interface to implement}
- `path/to/model` — Why: {schema structure to follow}
- `path/to/tests` — Why: {test pattern to replicate}

### New Files to Create

- `path/to/new_file` — {what it does}
- `tests/path/to/test_file` — {what it tests}

### Documentation

- [Link](url#section) — Why: {what you need from it}

### Patterns to Follow

**Naming:**

```
{actual naming examples from codebase}
```

**Error Handling:**

```
{actual error handling pattern from codebase}
```

**Imports:**

```
{actual import style from codebase}
```

---

## Implementation Tasks

Execute in order, top to bottom. Each task is atomic.

### Task 1: {Descriptive Name}

**File:** `path/to/file`
**Action:** CREATE | MODIFY
**Description:** {What to do and why}
**Depends on:** none
**Validate:** `{command to verify this task}`

### Task 2: {Descriptive Name}

**File:** `path/to/file`
**Action:** CREATE | MODIFY
**Description:** {What to do and why}
**Depends on:** Task 1
**Validate:** `{command to verify this task}`

{Continue for all tasks...}

---

## Testing Strategy

### Unit Tests

- **Where:** `tests/{path}/test_{name}.py` or `__tests__/{name}.test.ts`
- **Pattern:** Mirror existing test files at `{reference path}`
- **Minimum:** 1 happy path + 1 edge case + 1 error case per function

### Integration Tests

- {Describe cross-module or API-level tests needed}

### E2E Validation Steps

1. Start the application: `{command}`
2. {Step-by-step user journey with expected outcomes}
3. Verify: {what to check at each step}

---

## Validation Commands

### Level 1: Syntax & Types

```bash
{lint command}
{type-check command}
```

### Level 2: Unit Tests

```bash
{test command}
```

### Level 3: Build

```bash
{build command}
```

### Level 4: Manual Verification

{Exact steps: URLs, actions, expected results}

---

## Acceptance Criteria

- [ ] All tasks completed in dependency order
- [ ] All validation commands pass
- [ ] Manual verification confirms feature works
- [ ] Code follows project conventions from CLAUDE.md
- [ ] No regressions in existing functionality

---

## Contract

> This contract defines testable success criteria. It is negotiated during `/plan-feature` and graded by `/evaluate` after implementation. For Tier 2+, `/evaluate` runs both a Claude evaluator (formal contract grading) and a GPT-5.4 adversarial review (design challenge) in parallel. Both evaluators see ONLY this contract, the code diff, and CLAUDE.md — not the planning conversation.

### Tier

**{1 | 2 | 3}** — determines evaluation depth and model coverage:

| Tier | Claude Evaluator | Codex Adversarial (GPT-5.4 high) | Use When |
| ---- | ---------------- | -------------------------------- | -------- |
| 1    | 1 pass           | —                                | Bug fixes, simple endpoints, UI tweaks |
| 2    | 1 pass           | 1 `/codex:adversarial-review`    | New features, integrations, schema changes |
| 3    | Claude agent team | 1 `/codex:adversarial-review --effort xhigh` | Architectural changes, new pipeline phases |

### Success Criteria

```json
{
  "feature": "{Feature Name}",
  "tier": {1 | 2 | 3},
  "pass_threshold": 8,
  "criteria": [
    {
      "name": "{Specific, testable criterion}",
      "threshold": 8,
      "validation": "{How to verify — exact command, URL, or observable behavior}"
    },
    {
      "name": "{Another criterion}",
      "threshold": 9,
      "validation": "{How to verify}"
    }
  ]
}
```

**Rules for writing criteria:**

- Each criterion must be independently testable — no "works well" or "looks good"
- Include the validation method: a command to run, an API call to make, or an observable behavior
- **Thresholds: minimum 8, maximum 10.** Use 8 for standard functionality and UX criteria, 9 for correctness and regression-safety criteria, 10 for security, data integrity, secret handling, and multi-tenancy concerns. A threshold below 8 is never allowed — if it feels like a 7, the criterion is too vague to test or does not belong in the contract.
- `pass_threshold` on the top-level contract object must also be at least 8.
- Include at least one negative criterion (e.g., "Returns 403 when accessing another org's data")

---

## Adversarial Review

> Populated during `/plan-feature` Phase 5.5. Findings from fresh Claude sub-agent + Codex GPT-5.4 (Tier 2+) or self-critique (Tier 1) against the Karpathy four principles + PoetiQ cross-model verification. Critical findings are resolved or explicitly Acknowledged before the contract is drafted.

**Tier**: {1 | 2 | 3}
**Reviewers**: {"self-critique" | "Claude sub-agent + Codex GPT-5.4"}
**Refinement cycles**: {N}

### Critical (resolved before proceeding)

1. {dimension}: {specific weakness} — section: {heading} — resolution: {revised text | Acknowledged with reasoning}

### Consider (flagged, not acted on)

1. {dimension}: {specific weakness} — section: {heading}

### Reasoning divergence (alternative paths not taken)

1. {alternative approach} — why not: {reasoning}

---

## Notes

{Design decisions, trade-offs, potential follow-up work, known limitations}

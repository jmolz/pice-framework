# Feature: Pre-Launch Repo Polish

> Generated by `/plan-feature` — read CLAUDE.md and referenced files before implementing.

## Overview

Prepare the PICE CLI repo for a public Show HN launch by adding an example output section and FAQ to the README, creating issue templates, setting GitHub topics, enabling Discussions, and cutting a v0.1.0 release tag. No CLI code changes — this is entirely presentation and community infrastructure.

## User Story

As a developer discovering PICE CLI on Hacker News,
I want to see what it does immediately (example output), get my skeptical questions answered (FAQ), and have clear channels to engage (Discussions, issue templates),
so that I can evaluate whether PICE is worth trying and know how to participate if it is.

## Success Criteria

- [ ] README has an `## Example` section with realistic `pice evaluate` Tier 2 output
- [ ] README has a `## FAQ` section with 4 pre-emptive Q&A entries
- [ ] Three YAML issue templates exist in `.github/ISSUE_TEMPLATE/`
- [ ] Issue template config links to Discussions for questions
- [ ] GitHub repo has 9 topic tags
- [ ] GitHub Discussions is enabled
- [ ] v0.1.0 release exists with auto-generated notes
- [ ] CI badge remains green
- [ ] No existing tests broken

## Feature Metadata

- **Type**: Enhancement (repo polish, no code changes)
- **Complexity**: Low
- **Systems Affected**: README.md, `.github/` directory, GitHub repo settings
- **New Dependencies**: none

---

## Context References

### Must-Read Files (READ BEFORE IMPLEMENTING)

- `README.md` — The file being modified. Current structure: badges → What is PICE? → Installation → Quick Start → Commands → Architecture → Dual-Model → Configuration → Shell Completions → Telemetry → Contributing → License
- `crates/pice-cli/src/engine/output.rs` — The exact evaluation report format. Uses Unicode box-drawing characters (╔═╗║╠╣╚╝), ✅/❌ status icons, `{name:<28}` left-aligned criterion names, `{score:>2}/{threshold:<2}` right-aligned scores. Adversarial section shows `[severity] finding`. Overall shows `PASS ✅` or `FAIL ❌`.

### New Files to Create

- `.github/ISSUE_TEMPLATE/bug-report.yml` — Bug report form template
- `.github/ISSUE_TEMPLATE/feature-request.yml` — Feature request form template
- `.github/ISSUE_TEMPLATE/provider-request.yml` — AI provider request form template
- `.github/ISSUE_TEMPLATE/config.yml` — Template chooser config with Discussions link

### Patterns to Follow

**README section ordering convention:**
Sections flow from "what is it" → "how to install" → "how to use" → "how it works" → "how to configure" → "how to contribute". New sections must maintain this flow.

**GitHub YAML issue template format:**
```yaml
name: Template Name
description: One-line description
labels: [label1, label2]
body:
  - type: markdown
    attributes:
      value: "## Section header"
  - type: input
    id: field-id
    attributes:
      label: Field Label
      description: Help text
    validations:
      required: true
  - type: textarea
    id: field-id
    attributes:
      label: Field Label
  - type: dropdown
    id: field-id
    attributes:
      label: Field Label
      options:
        - Option 1
        - Option 2
```

---

## Implementation Tasks

Execute in order, top to bottom. Each task is atomic.

### Task 1: Add Example section to README

**File:** `README.md`
**Action:** MODIFY
**Description:** Insert a new `## Example` section between `## Quick Start` and `## Commands`. The section contains a brief intro sentence and a fenced code block showing realistic `pice evaluate` output for a Tier 2 evaluation.

The code block must match the exact output format from `output.rs`:

```
$ pice evaluate .claude/plans/auth-plan.md

╔══════════════════════════════════════╗
║       Evaluation Report — Tier 2      ║
╠══════════════════════════════════════╣
║ ✅ Auth endpoints return 401 ...  8/7  ║
║   All protected routes verified        ║
║ ✅ Password hashing uses bcrypt    9/7  ║
║   bcrypt with cost factor 12           ║
║ ✅ Session tokens expire after ... 8/8  ║
║   24h expiry confirmed in tests        ║
║ ✅ No secrets in git history       7/7  ║
║   Clean scan across all commits        ║
╠══════════════════════════════════════╣
║  Adversarial Review                    ║
║  [consider] Rate limiting on login ... ║
║  [consider] Token rotation strategy .. ║
╠══════════════════════════════════════╣
║  Overall: PASS ✅                      ║
║  All contract criteria met             ║
╚══════════════════════════════════════╝
```

IMPORTANT: The column widths in the example must be realistic (38-char inner width as in output.rs). Criterion names are truncated to 28 chars with `...` suffix if longer. Scores are right-aligned `{score:>2}/{threshold:<2}`. Use realistic auth-related criteria that tell a story.

The intro text before the code block should be one sentence: "Here's what a Tier 2 dual-model evaluation looks like after implementing a user authentication feature:"

**Depends on:** none
**Validate:** `grep -c "## Example" README.md` returns 1; visual inspection of formatting

### Task 2: Add FAQ section to README

**File:** `README.md`
**Action:** MODIFY
**Description:** Insert a new `## FAQ` section before `## Contributing`. Use H3 headings for each question. Four entries:

**### "Why not just use aider/cursor/copilot?"**

PICE is the orchestration layer, not a replacement for your AI coding tool. It works *with* tools like Claude Code, Cursor, or Copilot through a provider protocol — managing the lifecycle, enforcing contracts, and measuring quality while your preferred tool does the coding. Think of it as the CI/CD for AI coding sessions.

**### "Why Rust + TypeScript?"**

Rust for the CLI core — it's fast, compiles to a single binary, and handles process orchestration well. TypeScript for providers — AI SDKs (Anthropic, OpenAI) are JavaScript-first, and the provider protocol lets each side use its natural language. The two communicate over JSON-RPC on stdio.

**### "Is the telemetry sketchy?"**

No. Telemetry is opt-in and off by default. When enabled, it collects anonymous usage metrics (command frequency, evaluation pass rates, timing) — never code, prompts, or personal information. All telemetry data is written to `.pice/telemetry-log.jsonl` where you can inspect every event before anything leaves your machine.

**### "Does this actually improve code quality?"**

That's what the metrics engine is designed to answer. PICE tracks evaluation scores, pass rates, and workflow timing across your sessions so you can see whether structured workflows produce measurably better results than ad-hoc AI coding. Data over vibes.

**Depends on:** none
**Validate:** `grep -c "## FAQ" README.md` returns 1; all 4 questions present

### Task 3: Create issue template directory

**File:** `.github/ISSUE_TEMPLATE/` (directory)
**Action:** CREATE
**Description:** Create the `.github/ISSUE_TEMPLATE/` directory. The `.github/workflows/` directory already exists.

**Depends on:** none
**Validate:** `ls .github/ISSUE_TEMPLATE/`

### Task 4: Create Bug Report template

**File:** `.github/ISSUE_TEMPLATE/bug-report.yml`
**Action:** CREATE
**Description:** YAML form-based bug report template with these fields:

```yaml
name: Bug Report
description: Report a bug in PICE CLI
labels: [bug]
body:
  - type: input
    id: version
    attributes:
      label: PICE Version
      description: "Output of `pice --version`"
      placeholder: "0.1.0"
    validations:
      required: true
  - type: dropdown
    id: os
    attributes:
      label: Operating System
      options:
        - macOS (Apple Silicon)
        - macOS (Intel)
        - Linux (x64)
        - Linux (arm64)
        - Windows (x64)
        - Other
    validations:
      required: true
  - type: dropdown
    id: provider
    attributes:
      label: Provider
      description: "Which AI provider were you using?"
      options:
        - claude-code
        - codex
        - Other
        - N/A
    validations:
      required: true
  - type: textarea
    id: steps
    attributes:
      label: Steps to Reproduce
      description: "Minimal steps to trigger the bug"
      placeholder: |
        1. Run `pice evaluate plan.md`
        2. ...
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: Expected Behavior
    validations:
      required: true
  - type: textarea
    id: actual
    attributes:
      label: Actual Behavior
    validations:
      required: true
  - type: textarea
    id: logs
    attributes:
      label: Relevant Logs
      description: "Paste any error output (use `--verbose` for more detail)"
      render: shell
```

**Depends on:** Task 3
**Validate:** File exists and is valid YAML

### Task 5: Create Feature Request template

**File:** `.github/ISSUE_TEMPLATE/feature-request.yml`
**Action:** CREATE
**Description:**

```yaml
name: Feature Request
description: Suggest a new feature or enhancement
labels: [enhancement]
body:
  - type: textarea
    id: description
    attributes:
      label: Description
      description: "What would you like PICE to do?"
    validations:
      required: true
  - type: textarea
    id: use-case
    attributes:
      label: Use Case
      description: "Describe the problem this would solve or the workflow it would improve"
    validations:
      required: true
  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives Considered
      description: "Have you tried any workarounds or alternative approaches?"
```

**Depends on:** Task 3
**Validate:** File exists and is valid YAML

### Task 6: Create Provider Request template

**File:** `.github/ISSUE_TEMPLATE/provider-request.yml`
**Action:** CREATE
**Description:**

```yaml
name: Provider Request
description: Request support for a new AI coding tool
labels: [provider]
body:
  - type: input
    id: tool-name
    attributes:
      label: AI Tool Name
      description: "Which AI coding tool should PICE support?"
      placeholder: "e.g., Cursor, Copilot, Windsurf"
    validations:
      required: true
  - type: input
    id: sdk-link
    attributes:
      label: SDK / API Documentation
      description: "Link to the tool's SDK or API docs (if available)"
  - type: checkboxes
    id: capabilities
    attributes:
      label: Desired Capabilities
      description: "Which PICE commands should this provider support?"
      options:
        - label: "Workflow (plan, execute, review, commit)"
        - label: "Evaluation (evaluate — contract grading)"
        - label: "Both"
  - type: textarea
    id: context
    attributes:
      label: Additional Context
      description: "Anything else that would help (auth mechanism, SDK maturity, etc.)"
```

**Depends on:** Task 3
**Validate:** File exists and is valid YAML

### Task 7: Create template chooser config

**File:** `.github/ISSUE_TEMPLATE/config.yml`
**Action:** CREATE
**Description:**

```yaml
blank_issues_enabled: false
contact_links:
  - name: Ask a Question
    url: https://github.com/jmolz/pice-framework/discussions/categories/q-a
    about: Use GitHub Discussions for questions and help
```

**Depends on:** Task 3
**Validate:** File exists and is valid YAML

### Task 8: Set GitHub topics

**Action:** CLI command (not a file)
**Description:** Add topic tags to the repo for discoverability. Run:

```bash
gh repo edit jmolz/pice-framework --add-topic ai --add-topic cli --add-topic rust --add-topic typescript --add-topic developer-tools --add-topic code-quality --add-topic evaluation --add-topic ai-coding --add-topic workflow-orchestration
```

**Depends on:** none
**Validate:** `gh repo view jmolz/pice-framework --json repositoryTopics` shows all 9 topics

### Task 9: Enable GitHub Discussions

**Action:** CLI command (not a file)
**Description:** Enable Discussions on the repo via the GitHub GraphQL API:

```bash
# Get the repo node ID
REPO_ID=$(gh api graphql -f query='{ repository(owner:"jmolz", name:"pice-framework") { id } }' --jq '.data.repository.id')

# Enable discussions
gh api graphql -f query="mutation { updateRepository(input: { repositoryId: \"$REPO_ID\", hasDiscussionsEnabled: true }) { repository { hasDiscussionsEnabled } } }"
```

**Depends on:** none
**Validate:** `gh api repos/jmolz/pice-framework --jq '.has_discussions'` returns `true`

### Task 10: Create v0.1.0 release

**Action:** CLI commands (not a file)
**Description:** Tag the current HEAD and create a GitHub release with auto-generated notes. No binary uploads.

```bash
git tag v0.1.0
git push origin v0.1.0
gh release create v0.1.0 --title "v0.1.0 — Initial Release" --generate-notes
```

**Depends on:** Tasks 1-9 (do this last — after all file changes are committed and pushed)
**Validate:** `gh release view v0.1.0` shows the release; repo sidebar shows Releases section

### Task 11: Final CI verification

**Action:** Verification (no file changes)
**Description:** After all changes are pushed, verify CI is green:

```bash
gh run list --limit 1 --json status,conclusion --jq '.[0]'
```

Expected: `{ "status": "completed", "conclusion": "success" }`

**Depends on:** Task 10
**Validate:** CI badge on README renders green

---

## Testing Strategy

### Unit Tests

No new unit tests needed — this feature creates no CLI code.

### Integration Tests

No integration tests — all changes are static files and GitHub API calls.

### E2E Validation Steps

1. Visit `https://github.com/jmolz/pice-framework` — verify topics appear under repo name
2. Check README renders: Example section shows box-drawing output, FAQ section has 4 entries
3. Click "Issues" > "New Issue" — verify 3 template options + "Ask a Question" link appear
4. Click "Discussions" tab — verify it exists with default categories
5. Click "Releases" in sidebar — verify v0.1.0 appears with auto-generated notes
6. CI badge in README shows green

---

## Validation Commands

### Level 1: Syntax & Types

```bash
# No code changes, but verify README is valid markdown
# and YAML templates parse correctly
python3 -c "import yaml; [yaml.safe_load(open(f)) for f in __import__('glob').glob('.github/ISSUE_TEMPLATE/*.yml')]" 2>/dev/null || echo "YAML validation requires PyYAML"
```

### Level 2: Unit Tests

```bash
# Verify no regressions
cargo test && pnpm test
```

### Level 3: Build

```bash
# Verify build still works
cargo build --release && pnpm build
```

### Level 4: Manual Verification

1. `gh repo view jmolz/pice-framework --json repositoryTopics` — 9 topics
2. `gh api repos/jmolz/pice-framework --jq '.has_discussions'` — true
3. `gh release view v0.1.0` — release exists
4. `gh run list --limit 1` — CI green
5. Visual inspection of README on GitHub

---

## Acceptance Criteria

- [ ] All tasks completed in dependency order
- [ ] All validation commands pass
- [ ] Manual verification confirms all GitHub settings applied
- [ ] README reads well — Example is immediately compelling, FAQ is concise
- [ ] No regressions in existing functionality
- [ ] CI remains green after all changes

---

## Contract

> This contract defines testable success criteria. It is negotiated during `/plan-feature` and graded by `/evaluate` after implementation.

### Tier

**1** — No code changes, repo polish only. Single evaluator pass sufficient.

### Success Criteria

```json
{
  "feature": "Pre-Launch Repo Polish",
  "tier": 1,
  "pass_threshold": 7,
  "criteria": [
    {
      "name": "Example section present with eval output",
      "threshold": 7,
      "validation": "grep '## Example' README.md && grep 'Evaluation Report' README.md && grep 'Adversarial Review' README.md"
    },
    {
      "name": "FAQ section has all four entries",
      "threshold": 7,
      "validation": "grep -c '### ' README.md sections under ## FAQ count to 4; grep 'aider' README.md && grep 'Rust + TypeScript' README.md && grep 'telemetry' README.md && grep 'code quality' README.md"
    },
    {
      "name": "Three YAML issue templates exist",
      "threshold": 7,
      "validation": "ls .github/ISSUE_TEMPLATE/bug-report.yml .github/ISSUE_TEMPLATE/feature-request.yml .github/ISSUE_TEMPLATE/provider-request.yml"
    },
    {
      "name": "Issue config links to Discussions",
      "threshold": 7,
      "validation": "grep 'discussions' .github/ISSUE_TEMPLATE/config.yml"
    },
    {
      "name": "No blank issues allowed",
      "threshold": 7,
      "validation": "grep 'blank_issues_enabled: false' .github/ISSUE_TEMPLATE/config.yml"
    },
    {
      "name": "No regressions in test suite",
      "threshold": 8,
      "validation": "cargo test && pnpm test — all 217 tests pass"
    }
  ]
}
```

---

## Notes

- **GitHub API tasks (topics, discussions, release)** require `gh` CLI authenticated with repo admin permissions. The user (jmolz) is the repo owner, so this should work.
- **Release tag** should be created LAST, after all file changes are committed and pushed, so the tag points to the final state.
- **The example output** is a realistic approximation of the box-drawing format. It doesn't need to be pixel-perfect — it needs to be immediately compelling to a HN reader skimming the README.
- **HANDOFF.md** should be updated or deleted after this plan is executed, since it will resolve all remaining open items.

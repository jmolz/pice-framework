# Feature: Test Feature

> Generated by `/plan-feature` — read CLAUDE.md and referenced files before implementing.

## Overview

This implements a canonical "test feature" plan to validate the plan-creation and evaluation pipeline end-to-end within the PICE project. It produces a fully-conformant `.claude/plans/test-feature.md` document that serves as a reference example for plan structure, contract JSON authorship, and adversarial evaluation at Tier 1.

## User Story

As a PICE developer, I want a canonical example plan that exercises the full `/plan-feature` → `/evaluate` pipeline, so that I can confirm the planning workflow, contract parser, and evaluation runner operate correctly before relying on them for real feature plans.

## Success Criteria

- [ ] Plan file is present at `.claude/plans/test-feature.md` following every section of the plan-template exactly
- [ ] Contract section contains syntactically valid JSON with `tier`, `pass_threshold`, and at least 5 independently-testable `criteria`
- [ ] `pass_threshold` and every individual `threshold` are within the range 8–10 (inclusive)
- [ ] At least one criterion exercises a negative test path
- [ ] All Level 1–3 validation commands execute successfully against the existing codebase with zero errors
- [ ] No regressions in existing functionality (`cargo test` + `pnpm test` remain green)

## Feature Metadata

- **Type**: New Capability (plan scaffolding and pipeline validation)
- **Complexity**: Low
- **Systems Affected**: `.claude/plans/` (documentation artifact only — no production code changes)
- **New Dependencies**: none

---

## Context References

### Must-Read Files (READ BEFORE IMPLEMENTING)

- `.claude/templates/plan-template.md` — Why: authoritative section schema and Contract JSON rules to replicate exactly
- `.claude/plans/phase-3-seam-verification.md` (lines 1–120) — Why: most recent real Tier 3 plan; mirror its heading depth and Implementation Tasks style
- `.claude/plans/phase-2-providers.md` (lines 1–80) — Why: shows how Integration Tests and E2E steps are written for multi-task plans
- `CLAUDE.md` — Why: project-wide conventions that must be honoured in every plan and implementation

### New Files to Create

- `.claude/plans/test-feature.md` — this plan document (documentation only)

### Documentation

- [Plan Template](.claude/templates/plan-template.md) — Why: governs required sections and JSON contract structure
- [PICE Contract Rules](.claude/templates/plan-template.md#contract) — Why: threshold ranges, negative criteria requirement, evaluator model selection

### Patterns to Follow

**Naming:**

```
Plan files:      kebab-case, e.g. test-feature.md, phase-3-seam-verification.md
Section headers: Title Case, e.g. ## Implementation Tasks
Task headings:   ### Task N: Descriptive Name
```

**Error Handling:**

```
Not applicable — documentation-only artifact; no executable code paths.
```

**Imports:**

```
Not applicable — documentation-only artifact.
```

---

## Implementation Tasks

Execute in order, top to bottom. Each task is atomic.

### Task 1: Create Plan File

**File:** `.claude/plans/test-feature.md`
**Action:** CREATE
**Description:** Write this plan document filling every section of the plan-template: Overview, User Story, Success Criteria, Feature Metadata, Context References, Implementation Tasks, Testing Strategy, Validation Commands, Acceptance Criteria, Contract, and Notes. No section may be omitted or left with placeholder text.
**Depends on:** none
**Validate:** `ls .claude/plans/test-feature.md && echo "File exists"`

### Task 2: Validate Section Coverage

**File:** `.claude/plans/test-feature.md` (read-only check)
**Action:** VERIFY
**Description:** Count top-level `##` headings to confirm all required sections are present. The plan-template requires at least 8 distinct `##` sections.
**Depends on:** Task 1
**Validate:** `grep -c "^## " .claude/plans/test-feature.md | xargs -I{} sh -c 'test {} -ge 8 && echo "Section count OK ({})" || (echo "FAIL: only {} sections" && exit 1)'`

### Task 3: Validate Contract JSON

**File:** `.claude/plans/test-feature.md` (Contract section)
**Action:** VERIFY
**Description:** Extract the JSON fenced code block from the `## Contract` section and parse it with Python's `json` module to confirm zero syntax errors, required top-level keys (`feature`, `tier`, `pass_threshold`, `criteria`), and threshold range compliance.
**Depends on:** Task 2
**Validate:**
```bash
python3 -c "
import re, json, sys
content = open('.claude/plans/test-feature.md').read()
m = re.search(r'\`\`\`json\n(.*?)\n\`\`\`', content, re.DOTALL)
if not m:
    sys.exit('ERROR: No JSON block found in Contract section')
data = json.loads(m.group(1))
for key in ('feature', 'tier', 'pass_threshold', 'criteria'):
    if key not in data:
        sys.exit(f'ERROR: missing key: {key}')
if data['pass_threshold'] < 8:
    sys.exit('ERROR: pass_threshold must be >= 8')
for c in data['criteria']:
    if not (8 <= c['threshold'] <= 10):
        sys.exit(f'ERROR: criterion threshold out of range: {c}')
print('Contract JSON is valid')
"
```

### Task 4: Confirm No Production Code Changes

**File:** (repository working tree)
**Action:** VERIFY
**Description:** Confirm the git diff contains no files outside `.claude/plans/`. This plan is documentation-only and must not touch production code.
**Depends on:** Task 1
**Validate:** `git diff --name-only HEAD | grep -v "^\.claude/plans/" | xargs -I{} sh -c 'echo "UNEXPECTED CHANGE: {}" && exit 1' || echo "No production code changes — OK"`

---

## Testing Strategy

### Unit Tests

- **Where:** Not applicable — plan files are documentation artifacts with no executable code.
- **Pattern:** N/A
- **Minimum:** N/A

### Integration Tests

- After creating the plan, manually invoke `pice evaluate --plan .claude/plans/test-feature.md` (when the daemon is running) and confirm:
  - The daemon parses the `## Contract` section without errors.
  - The evaluation exits with code 0 (all criteria pass) or code 2 (criteria fail) — never code 1 (unexpected crash).
- Confirm the contract parser in `pice-core::plan_parser` correctly extracts the `## Contract` JSON block from this file (existing unit tests in `crates/pice-core/src/plan_parser/` cover this path).

### E2E Validation Steps

1. Verify file exists: `ls -la .claude/plans/test-feature.md` → expect non-zero file size
2. Verify section count: `grep -c "^## " .claude/plans/test-feature.md` → expect `≥ 8`
3. Verify Contract JSON validity: run the Python snippet from Task 3 → expect `Contract JSON is valid`
4. Verify no production code changes: `git diff --name-only HEAD` → must show only `.claude/plans/test-feature.md`
5. (Optional, daemon required) Run `pice evaluate --plan .claude/plans/test-feature.md --json` → expect JSON output with no `"error"` key at top level

---

## Validation Commands

### Level 1: Syntax & Types

```bash
# Verify the file is present
ls .claude/plans/test-feature.md

# Verify all required section headings exist
grep "^## " .claude/plans/test-feature.md
```

### Level 2: Unit Tests

```bash
# Validate embedded Contract JSON block
python3 -c "
import re, json, sys
content = open('.claude/plans/test-feature.md').read()
m = re.search(r'\`\`\`json\n(.*?)\n\`\`\`', content, re.DOTALL)
if not m:
    sys.exit('ERROR: No JSON block found')
data = json.loads(m.group(1))
for key in ('feature', 'tier', 'pass_threshold', 'criteria'):
    if key not in data:
        sys.exit(f'ERROR: missing required key: {key}')
if data['pass_threshold'] < 8:
    sys.exit('ERROR: pass_threshold must be >= 8')
for c in data['criteria']:
    if not (8 <= c['threshold'] <= 10):
        sys.exit(f'ERROR: criterion threshold out of range: {c}')
print('Contract JSON is valid — all thresholds in range, all required keys present')
"
```

### Level 3: Build

```bash
# No build step required — documentation only.
# Run full project validation to confirm no regressions:
cargo fmt --check && cargo clippy -- -D warnings && cargo test
pnpm lint && pnpm typecheck && pnpm test
```

### Level 4: Manual Verification

1. Open `.claude/plans/test-feature.md` in any markdown viewer.
2. Confirm all top-level sections render without broken headings or placeholder text: Overview, User Story, Success Criteria, Feature Metadata, Context References, Implementation Tasks, Testing Strategy, Validation Commands, Acceptance Criteria, Contract, Notes.
3. Confirm the Contract JSON block is syntactically valid and contains at least 5 criteria, all with `threshold ≥ 8`.
4. Confirm at least one criterion tests a negative / rejection path.
5. Confirm `"tier": 1` is set (documentation-only, no adversarial Codex review needed).

---

## Acceptance Criteria

- [ ] All tasks completed in dependency order (Task 1 → Task 2 → Task 3 → Task 4)
- [ ] All validation commands pass: file exists, section count ≥ 8, JSON valid, no production code changes
- [ ] Manual verification confirms plan renders cleanly with no missing or placeholder sections
- [ ] Code follows project conventions from CLAUDE.md (kebab-case filenames, Title Case section headers, `### Task N:` task headings)
- [ ] No regressions in existing functionality (`cargo test` baseline 599 tests green; `pnpm test` baseline 54 tests green)

---

## Contract

> This contract defines testable success criteria. It is negotiated during `/plan-feature` and graded by `/evaluate` after implementation. For Tier 2+, `/evaluate` runs both a Claude evaluator (formal contract grading) and a Codex adversarial review in parallel. Both evaluators see ONLY this contract, the code diff, and CLAUDE.md — not the planning conversation.

### Tier

**1** — determines evaluation depth and model coverage:

| Tier | Claude Evaluator | Codex Adversarial (GPT-4.1 high) | Use When |
| ---- | ---------------- | -------------------------------- | -------- |
| 1    | 1 pass           | —                                | Bug fixes, simple endpoints, UI tweaks, documentation artifacts |
| 2    | 1 pass           | 1 `/codex:adversarial-review`    | New features, integrations, schema changes |
| 3    | Claude agent team | 1 `/codex:adversarial-review --effort xhigh` | Architectural changes, new pipeline phases |

### Success Criteria

```json
{
  "feature": "Test Feature",
  "tier": 1,
  "pass_threshold": 8,
  "criteria": [
    {
      "name": "Plan file exists at the correct path",
      "threshold": 8,
      "validation": "Run: ls .claude/plans/test-feature.md — must exit 0 and print the file path without error"
    },
    {
      "name": "Plan contains all required top-level sections",
      "threshold": 8,
      "validation": "Run: grep -c '^## ' .claude/plans/test-feature.md — must return 8 or more, covering Overview, User Story, Success Criteria, Feature Metadata, Context References, Implementation Tasks, Testing Strategy, Validation Commands, Acceptance Criteria, Contract, Notes"
    },
    {
      "name": "Contract JSON block is syntactically valid and contains required keys",
      "threshold": 9,
      "validation": "Run the Python validation snippet in Validation Commands Level 2 — must print 'Contract JSON is valid' and exit 0; must not raise json.JSONDecodeError or KeyError"
    },
    {
      "name": "Contract pass_threshold is at least 8",
      "threshold": 9,
      "validation": "Parse the JSON block and assert data['pass_threshold'] >= 8; a value below 8 violates plan-template rules and must cause the check to fail"
    },
    {
      "name": "Every individual criterion threshold is between 8 and 10 inclusive",
      "threshold": 9,
      "validation": "Parse the criteria array; for each item assert 8 <= item['threshold'] <= 10; any out-of-range value fails the criterion"
    },
    {
      "name": "Plan tier is set to 1 (documentation-only artifact, no adversarial review needed)",
      "threshold": 8,
      "validation": "Parse the JSON block and assert data['tier'] == 1; setting tier > 1 would trigger unnecessary Codex adversarial review for a docs-only change"
    },
    {
      "name": "Plan does not introduce any production code changes outside .claude/plans/",
      "threshold": 9,
      "validation": "Run: git diff --name-only HEAD | grep -v '^.claude/plans/' — output must be empty; any non-empty line is an unexpected production code change and fails this criterion"
    },
    {
      "name": "Full test suite remains green after adding this plan (regression safety)",
      "threshold": 9,
      "validation": "Run: cargo test && pnpm test — both must exit 0 with counts matching the established baseline (≥599 Rust tests, ≥54 TS tests); any new failure is a regression"
    },
    {
      "name": "Plan file must NOT exist at an incorrect location (negative test)",
      "threshold": 8,
      "validation": "Run: ls .claude/test-feature.md 2>/dev/null && echo FAIL || echo PASS — must print PASS, confirming the file was not accidentally created outside the plans/ subdirectory"
    }
  ]
}
```

**Rules for writing criteria:**

- Each criterion must be independently testable — no "works well" or "looks good"
- Include the validation method: a command to run, an API call to make, or an observable behavior
- **Thresholds: minimum 8, maximum 10.** Use 8 for standard functionality and UX criteria, 9 for correctness and regression-safety criteria, 10 for security, data integrity, secret handling, and multi-tenancy concerns. A threshold below 8 is never allowed — if it feels like a 7, the criterion is too vague to test or does not belong in the contract.
- `pass_threshold` on the top-level contract object must also be at least 8.
- Include at least one negative criterion (e.g., "Returns 403 when accessing another org's data")

---

## Notes

- **Tier 1** is appropriate here because this is a documentation-only artifact — no production code is changed, no new runtime behaviour is introduced, and the risk surface is limited to markdown formatting and JSON syntax.
- The `pice-core::plan_parser` module already has unit tests that exercise `## Contract` section extraction; this plan's contract JSON is therefore implicitly covered by the existing test suite without requiring new test files.
- If this plan is ever adapted as a seed for a real feature, promote the Tier to 2 or 3, expand the criteria to cover runtime behaviour (API responses, state transitions, error paths), and add security-boundary negative criteria at threshold 10.
- The negative criterion ("plan NOT misplaced outside `plans/`") is intentionally lightweight to satisfy the template requirement; higher-tier plans should include negative criteria covering authentication failures, access-control boundaries, and invalid input rejection.
- No follow-up work is anticipated unless the `/evaluate` pipeline reports issues with contract JSON parsing on this file.
